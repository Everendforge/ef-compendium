# Contributing

Keep the Compendium read-only over source vaults. Changes to portable vault or PathBranching input contracts belong in `Everendforge/specs` first. Run `npm run typecheck && npm run lint && npm test` before opening a pull request.
